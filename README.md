# AI Code Reviewer

A GitHub PR bot that reviews diffs, flags issues, and blocks unsafe merges.

## Setup

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install pytest
```

## Run tests

```bash
pytest tests/
```
